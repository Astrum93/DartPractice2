void main() {
  /// Dart 기초 예제 연습2
  /// 분기문과 반복문
  ///
  /// 분기문 : 프로그램이 특정한 상황에 일을 할 지 / 하지 않고 넘길지
  /// if / else || switch

  /// if문 / if - else 문
  /// if (조건식) {
  ///   조건식이 참일 경우 실행 할 코드
  /// } else {
  ///   조건식이 거짓일 경우 실행 할 코드
  /// }
  ///

  bool isTrue = true;
  String text = 'Hello';

  if (isTrue) {
    print('True');
  }

  if (text == 'Hello') {
    print('True');
  }
}
